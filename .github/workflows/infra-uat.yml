name: "Infrastructure - UAT"

on:
  push:
    branches: [main]
    paths: [infra/**]
  pull_request:
    paths: [infra/**]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  uat:
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra/svc-blob-storage
      environment: uat
      project_name: blobdemo
      service_name: svc-blob-storage
    secrets: inherit
